name: 'Setup GDAL'
description: 'Install GDAL via system package manager'

runs:
  using: 'composite'
  steps:
    - name: Install GDAL
      shell: bash
      run: ./scripts/install-gdal.sh

    - name: Verify GDAL installation
      shell: bash
      run: gdal-config --version
